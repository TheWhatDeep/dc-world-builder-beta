{
  "name": "codex-server",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for CODEX — relational store, accounts, multi-user self-hosting",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.1.1",
    "better-sqlite3": "^11.8.1",
    "fastify": "^5.2.1",
    "sharp": "^0.33.5"
  }
}
